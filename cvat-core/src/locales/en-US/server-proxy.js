const serverProxy = {
    "Could not create the task on the server.": "Could not create the task on the server.",
    "Unknown task state has been received: ${response.data.state}": "Unknown task state has been received: ${response.data.state}",
    "The task is being created on the server..": "The task is being created on the server..",
    "The data are being uploaded to the server..": "The data are being uploaded to the server..",
    "Could not get preview frame for the task ${tid} from the server": "Could not get preview frame for the task ${tid} from the server"
}

module.exports = serverProxy;