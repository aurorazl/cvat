module.exports = {
    "Extra opening bracket found": "Extra opening bracket found",
    "Extra closing bracket found": "Extra closing bracket found",
    "Argument must be an array of strings": "Argument must be an array of strings",
    "Wrong filter expression. ${error.toString()}": "Wrong filter expression. ${error.toString()}",
    "Could not apply the filter. ${error.toString()}": "Could not apply the filter. ${error.toString()}"
}