module.exports = {
    "Argument task is expected to be an instance of Task class, but got ${typeof (task)}": "参数task应该是Task类的实例，但是得到了{{typeoftask}}",
    "Argument model is expected to be an instance of MLModel class, but got ${typeof (model)}": "参数model应该是MLModel类的实例，但是得到的是{{typeofmodel}}",
    "Argument args is expected to be an object, but got ${typeof (model)}": "参数args应该是一个object类型，但是得到的是{{typeofmodel}}",
    "Request id argument is required to be a string. But got ${requestID}": "请求id参数要求是字符串类型。但是得到的是{{requestID}}",
    "Could not get a status of the request ${requestID}. ${error.toString()}": "无法获取请求{{requestID}}的状态。{{errortoString}}",
}