export default {
  'Repository status check failed.': 'Repository status check failed.',
  'Repository status check returned the status "${response.status}"': 'Repository status check returned the status "${response.status}"',
  'Can not sent a request to clone the repository. ${error.toString()}': 'Can not sent a request to clone the repository. ${error.toString()}',
  'The repository is being cloned..': 'The repository is being cloned..',
  'Plugin allows to work with git repositories': 'Plugin allows to work with git repositories',
  'Can not push to remote repository. Message: ${response.stderr}': 'Can not push to remote repository. Message: ${response.stderr}',
  'Check returned status "${response.status}".': 'Check returned status "${response.status}".',
}